# 步骤 8：画面匹配指南

## 核心目标

步骤 8 不负责剧情编辑决策，只负责：
- 为 `storyboard.json` 中所有 `mode = voiceover` 且尚未写时间的段落匹配画面
- 保证画面时长覆盖对应 `audio_duration_sec`
- 让匹配结果服务于**情绪推进**，而不是只做到“相关”

`mode = original` 的段落在步骤 6 已通过字幕 / transcript 锚定精确时间。步骤 8 只在缺失或需要校正时补写。

## 输入

匹配前必须提供：
- 当前 source 对应视频路径
- transcript 时间轴摘要
- `storyboard.json` 中待匹配的 `voiceover` 片段
- 对应片段的 `audio_duration_sec`
- 当前 source 已占用时间段

## 匹配原则

### 必须优先的画面
- 有明显表情变化的画面
- 有肢体动作、冲突动作、停顿、回头、沉默、对视的画面
- 能承载“羞辱 / 反抗 / 心碎 / 安慰 / 反转 / 站队”情绪的画面

### 尽量避免的画面
- 纯说明性过场镜头
- 只是“人物在场”，但没有情绪信息的空镜头
- 虽然语义相关，但情绪承载力很弱的画面

### 匹配判断标准

好的匹配理由应该像这样：
- “孩子回头安慰妈妈的表情和动作能承载落难后的情绪缓冲”
- “婚宴众人围观的场面能给后一句站队台词制造压迫感”

不好的匹配理由像这样：
- “画面和文案相关”
- “该场景出现在这段时间” 

## subagent prompt 规范

派发给 `doubao-chat` 的 prompt 必须明确：
- 只匹配 `voiceover` 段
- 输出只包含：`id`、`source_index`、`start`、`end`、`notes`
- `notes` 必须写清：为什么这段画面能承载该句 VO 的情绪和叙事作用

示例输出：

```json
[
  {
    "id": "seg_08",
    "source_index": 2,
    "start": "00:01:56.180",
    "end": "00:02:05.900",
    "notes": "孩子安慰妈妈的表情和台词能承接上一个离家段落，适合作为落难后的情绪缓冲。"
  }
]
```

## 强制校验规则

1. 所有待匹配 `voiceover` 段都必须返回一条时间结果
2. `end - start` 必须覆盖 `audio_duration_sec`
3. 同一 `source_index` 内所有已占用时间段不能重叠
4. 时间不能越界
5. 若匹配结果只有语义相关、没有情绪承载价值，应视为低质量匹配并返工

## 音频超长修复流程

当某段 `voiceover` 的音频时长大于匹配画面时长：
1. 先尝试延长时间范围
2. 若会与其它片段冲突，则回退到步骤 6，缩短该段 VO 文案
3. 重新生成配音并重新匹配

## 回写规则

匹配完成后：
- 必须把 `start`、`end`、`notes` 回写到 `storyboard.json`
- 可选生成 `analysis/scene_matching.json` 审查快照
- 不允许把 `scene_matching.json` 当作新的真相源
